// Same-tail top-level DECOY sharing the nested NOuter.NInner's simple name. The §1(3)(b) inc-11
// OUTER-first oracle must resolve a dotted param type NOuter.NInner to the NESTED type, never this
// decoy (decoy-safe — SDD-004 §1(3)).
public class NInner {}
