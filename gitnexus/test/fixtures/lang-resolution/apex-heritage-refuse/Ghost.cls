// Same-tail top-level decoy for the external-OUTER and outer-found/tail-absent refuse shapes:
// the seam must NEVER let a dotted base fall through to the QNI dotted-tail fallback and bind this.
public class Ghost {
    public void haunt() {}
}
