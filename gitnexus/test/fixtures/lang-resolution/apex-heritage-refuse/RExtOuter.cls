// Dotted base, EXTERNAL/absent OUTER (Ext is not user-defined): seam state-(ii) refuse -> no EXTENDS
// edge, and NEVER the same-tail top-level decoy Ghost. Pre-WI-4 the shared QNI dotted-tail fallback
// mis-binds Ghost (the BL-4 pattern); the seam gates that fallback. SDD-004 §4.
public class RExtOuter extends Ext.Ghost {}
