// Same-tail top-level decoy for the case-collided-OUTER refuse shape: the seam must refuse (OUTER
// inject-none-suppressed to 0 candidates) rather than fall through to bind this decoy.
public class CInner {
    public void trick() {}
}
