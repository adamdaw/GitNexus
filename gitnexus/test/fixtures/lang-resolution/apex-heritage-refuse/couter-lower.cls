// The case-variant twin of COuter (folds to the same 'couter' key) -> SDD-003 §3 inject-none.
// Filename is couter-lower.cls (not couter.cls) to avoid the on-disk case collision with its twin
// couter-upper.cls; the class name (couter) is what folds, not the filename.
public virtual class couter {}
