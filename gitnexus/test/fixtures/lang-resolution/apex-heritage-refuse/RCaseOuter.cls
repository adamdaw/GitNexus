// Dotted base, CASE-VARIED OUTER (HOUTER.HInner, valid Apex): the OUTER segment is matched
// case-folded (REQ-005), binding HOuter exactly as HOuter.HInner would, and the nested tail is
// likewise folded -> seam state-(i) RESOLVE to the nested HInner (the dotted analogue of BL-1's
// case-varied simple extends). SDD-004 §4.
public class RCaseOuter extends HOUTER.HInner {}
