// A CLASS misfiled in a .trigger file (invalid Apex): the extension discriminant
// excludes it from injection -> documented liveness limitation, never a mis-bind.
public class Rogue {
    public void sneak() {}
}
