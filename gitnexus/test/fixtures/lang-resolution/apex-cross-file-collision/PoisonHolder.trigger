// A class misfiled in a .trigger file, sharing its name with the valid Poison.cls: the
// extension filter removes this def BEFORE grouping, so no guard collision forms.
public class Poison {
    public void bad() {}
}
